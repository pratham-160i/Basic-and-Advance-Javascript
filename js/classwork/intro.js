/*Create a file called intro.js (or use JSFiddle).
· Declare const variables for: your name, your college/university, your favourite subject, and one thing you
hope to build with JavaScript someday.
· Use console.log() to print each one with a label — e.g.: console.log('Name:', yourName)
Task 2 Experiment with Reassignment
· Declare a let variable called currentMood and set it to 'Focused'.
· Log it. Then change it to 'Curious'. Log it again.
· Now try to do the same with a const variable. What happens? Paste the error message in the chat.
Task 3 Naming Practice (Spot the Mistake)
· The following variable names are incorrect for JavaScript convention. Fix them:
· first_name → fix to camelCase
· TotalScore → this is PascalCase, fix to camelCase
· x → rename to something descriptive like 'maxLoginAttempts'
Bonus Write a Comment Story
· Declare at least 5 variables that tell a mini-story (can be fictional). Every variable should have a single-line
comment explaining what it represents.
· Example: const heroName = 'Arya'; // The protagonist of our story */

const name = "Gokul"
const university = "CUSAT"
const favouriteSubject = "Physics"
const hopeToBuild = "Website"

console.log("Name:",name);
console.log("University:",university);
console.log("Favourite Subject:",favouriteSubject);
console.log("I hope to build a:",hopeToBuild);

let currentMood;
console.log(currentMood);
currentMood = "focused";
console.log(currentMood);


//· first_name → fix to camelCase
let firstName

//· TotalScore → this is PascalCase, fix to camelCase
let totalScore

